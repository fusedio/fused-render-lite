"""Self-update machinery for the packaged macOS app (`mac`), on the
platform-neutral signed-manifest core (`common`) and a stdlib-only Ed25519
(`ed25519`). Ported from fused-render's `fused_render/update/`; see mac.py's
module docstring for what changed."""
